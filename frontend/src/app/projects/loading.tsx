import { ResearchListingView } from "@/components/research-listing";

export default function Loading() {
  return <ResearchListingView description="Active and completed research programs, their objectives, collaborators, and linked outputs." eyebrow="What we are building" loading title="Projects" type="PROJECT" />;
}
