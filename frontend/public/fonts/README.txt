The AMIRLab frontend expects the existing user-supplied file:

  frontend/public/fonts/ff-mab.regular.ttf

Keep/copy that exact brand font file from the original AMIRLab repository into this directory before running the frontend. The stylesheet already references it.
